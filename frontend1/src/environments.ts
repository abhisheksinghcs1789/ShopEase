// Simple single-file environment config (skipping Angular's environments/
// file-replacement setup to keep the project easy to read for a first pass).
// Point this at your deployed backend URL when you go to production.
export const environment = {
  apiUrl: 'http://localhost:5000/api',
};
